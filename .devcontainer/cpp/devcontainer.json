// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/ubuntu
{
	"name": "cpp",
	"image": "mcr.microsoft.com/devcontainers/base:resolute",
	"features": {
		"ghcr.io/devcontainer-community/devcontainer-features/collection-c-cpp:1": {}
	},
	"postCreateCommand": "sudo apt-get update && sudo apt-get install -y python3-pip && python3 -m pip install --user --break-system-packages 'conan>=2,<3' && conan profile detect --force",
	"customizations": {
		"vscode": {
			"settings": {
				"clangd.arguments": [
					"--compile-commands-dir=${userHome}/.cpp"
				],
				"cmake.copyCompileCommands": "${userHome}/.cpp/compile_commands.json",
				"cmake.ignoreCMakeListsMissing": true,
				"cmake.sourceDirectory": "${workspaceFolder}/cpp",
				"cmake.useCMakePresets": "auto"
			},
			"extensions": [
				"llvm-vs-code-extensions.vscode-clangd",
				"ms-vscode.cmake-tools",
				"ms-vscode.cpptools"
			]
		}
	},
	"remoteEnv": {
		"DEBUGINFOD_URLS": ""
	}
}
